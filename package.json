{
  "name": "trtime",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist/ && esbuild src/index.ts --bundle --minify --outfile=dist/index.js && tsc -emitDeclarationOnly --declaration",
    "prepare": "pnpm run build",
    "lint": "eslint --max-warnings=0 src/",
    "format": "prettier --write src/",
    "type-lint": "tsc --noEmit",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.4.0",
    "@typescript-eslint/parser": "^6.4.0",
    "esbuild": "^0.19.2",
    "eslint": "^8.47.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.0.2",
    "typescript": "^5.1.6",
    "vitest": "^0.34.3"
  },
  "packageManager": "pnpm@8.6.12",
  "engines": {
    "node": ">=18.0.0"
  }
}
